import styled from '@emotion/styled';

const Wrapper = styled.div`
  background-color: pink;
  height: 100px;
`;

export default function LayoutFooter(): JSX.Element {
  return <Wrapper>footer입니다.~~~~</Wrapper>;
}
